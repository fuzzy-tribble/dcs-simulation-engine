# TODO: pre-live-release - discuss design decisions

**1) Why'd you use LangGraph instead of LangChain?**

**2) Why didn't you use langchain's built in ChatModel classes?**

**3) Why did you implement a custom langgraph declarative builder (ie. graph.py)?**